---
layout: ../../layouts/blog-post.astro
title: "dependabot の cooldown と pnpm の minimumReleaseAge を設定する"
emoji: 🛡️
date: 2025-09-28T16:37:18Z
tags:
    - note
---

直近で話題になることが多かったサプライチェーン攻撃に対する予防策として、「一定時間経過したパッケージしかアップデート対象にしない」というオプションが dependabot や pnpm に導入されている。

- https://github.blog/changelog/2025-07-01-dependabot-supports-configuration-of-a-minimum-package-age/
- https://pnpm.io/ja/blog/releases/10.16
- https://pnpm.io/ja/blog/releases/10.17

## dependabot / cooldown 

- https://docs.github.com/ja/code-security/dependabot/dependabot-version-updates/optimizing-pr-creation-version-updates#setting-up-a-cooldown-period-for-dependency-updates

`.github/dependabot.yml` に次のように設定する:

```yaml
version: 2
updates:
  - package-ecosystem: "npm"
    directory: "/workers"
    cooldown:
      default-days: 14
      semver-major-days: 30
```

`semver-major-days` などで semantic versioning に従って細かく設定ができる。`include / exclude` での除外設定もサポートされている。

## pnpm / minimumReleaseAge

- https://pnpm.io/ja/settings#minimumreleaseage

`pnpm-workspace.yaml` に次のように設定するか、CLI からも設定できる:

```yaml
minimumReleaseAge: 10080
```

```shell
$  pnpm config set --location=project minimumReleaseAge 10080
```

dependabot とは違い、こちらは時間単位での指定。 `10.17.0` で `minimumReleaseAgeExclude` が入って除外設定もできるようになった。